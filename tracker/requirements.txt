# Dépendances Python (offline-friendly).
#
# NOTE : picamera2 N'EST PAS ici — il s'installe via apt (python3-picamera2),
#        cf. setup.sh. Il n'est pas disponible/pip-installable sur Windows.
#
# opencv-python-headless : suffisant pour le pipeline + flux MJPEG (pas de fenêtre).
#   -> Pour utiliser l'affichage local cv2.imshow (output.display: true),
#      remplacer par "opencv-contrib-python" (fenêtre + trackers CSRT legacy).

ultralytics>=8.4.0          # YOLO11/YOLO26 + export NCNN (8.4.63 au 2026-06)
lapx>=0.5.11             # solveur d'assignation (lap.lapjv) requis par ByteTrack ; lapx = fork ARM-friendly
                         #   (NE PAS installer "lap" : il casse sur ARM -> AttributeError lapjv)
opencv-python-headless>=4.10.0
flask>=3.0.0
numpy>=1.26.0
pyyaml>=6.0
loguru>=0.7.0
pyserial>=3.5             # contrôleur de focus SCF4 de la caméra C3 18X (camera.type: c3)
gpiozero>=2.0            # servos pan/tilt sur Raspberry Pi (pantilt.driver: servo). Import paresseux :
                         #   inutile en dev (pantilt.driver: sim) -> repli auto si absent.
